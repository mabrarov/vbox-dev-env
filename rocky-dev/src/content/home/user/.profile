#!/bin/bash

. "${HOME}/.local/j.sh" "${HOME}/.config/JetBrains/IntelliJIdea" idea /opt/ideaIU
. "${HOME}/.local/j.sh" "${HOME}/.config/JetBrains/Rider" rider /opt/rider
. "${HOME}/.local/j.sh" "${HOME}/.config/JetBrains/GoLand" goland /opt/goland
. "${HOME}/.local/j.sh" "${HOME}/.config/JetBrains/CLion" clion /opt/clion
